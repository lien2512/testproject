import { CustomAvatarDirective } from './custom-avatar.directive';

describe('CustomAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
